# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: com.fortify.demo.zui5fiori
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://vhcala4hci:50000
          client: '001'
        app:
          name: ZCARD_TXN_APP
          description: Card Transaction Fiori App
          package: Z_FIORI_APP
          transport: A4HK900168
        exclude:
          - /test/
